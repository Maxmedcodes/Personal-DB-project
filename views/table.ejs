<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="main.css">
    <title>MY SQL DB</title>
    <style>
    table, th, td {
        border:1px solid black;
    }
    </style>
</head>
<body>
    <h1>Testing SQL DB</h1>
    
    <table>
        <thead>
          <tr>
            <th>ID</th>
            <th>User Name</th>
            <th>Country</th>
            <th>Country Code</th>
          </tr>
        </thead>
        <tbody>
            <% entries.forEach((entry, index) => { %>
              <tr>
                <!-- Increment ID dynamically based on index -->
                <td><%= index + 1 %></td>
                <td><%= entry.user_name %></td>
                <td><%= entry.countries %></td>
                <td><%= entry.country_code %></td>
              </tr>
            <% }); %>
          </tbody>>
      </table>

</script>      
</body>
</html>